{
  "cli": {
    "version": ">= 23.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "base": {
      "node": "22.21.1"
    },
    "development": {
      "extends": "base",
      "developmentClient": true,
      "distribution": "internal",
      "ios": {
        "simulator": true
      },
      "_comment": "No EXPO_PUBLIC_API_BASE_URL on purpose: a dev client derives the API host from the Metro server it connects to (src/api/client.ts), so it follows this machine across DHCP leases instead of being pinned to one that expires. preview and production DO pin it, because a shipped build must never guess."
    },
    "preview": {
      "extends": "base",
      "distribution": "internal",
      "env": {
        "EXPO_PUBLIC_API_BASE_URL": "https://awardhome.com"
      }
    },
    "production": {
      "extends": "base",
      "autoIncrement": true,
      "env": {
        "EXPO_PUBLIC_API_BASE_URL": "https://awardhome.com"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
