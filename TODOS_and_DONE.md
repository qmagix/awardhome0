# TODOS and DONE

## TODO

### Release
- [x] (SHIPPED 2026-09-04) **Partner API** (`/api/v1/partner`, dark behind `partner_api` flag): keyed exact-match dancer lookups for vetted partners (schools verifying applicants). Keys hash-only at `/admin/partner-keys` (issued only with a data-agreement reference), append-only audit log = quota ledger = "who looked up my child" answer, 60/min burst + per-key daily quota, visibility identical to the public dancer page (suppression, hide_from_search, hidden cards, verification_status exposed), numeric ids refused. Contract test `test/api_partner.js` = gate stage 3. Data agreement DRAFTED 2026-09-04 (`docs/partner_data_agreement.md` — needs attorney review, esp. FCRA §8) with pricing decided: $10 per 100 dancer records, prepaid blocks, unique-record-per-month metering (admin page shows billable count from the audit log). TODO before first key: attorney pass on the agreement, then flip the flag. Pilot credit enforcement is manual (quota/revoke); build automatic credit balances if partner count grows. See features.md §1b.
- [x] (SHIPPED 2026-09-04) **Safety suppression** (`utils/suppression.js`, `/admin/suppressions`): superadmin can hide a dancer from every public surface on a family's safety request; suppressed reads as nonexistent (404, absent from search/rankings/cast lists/widget/card embeds/mobile guest reads), family keeps its own signed-in views, flag survives all merge paths. Smoke + mobile-API contract coverage; gate's adversarial DB now suppresses group-award members and sweeps castmate/studio render paths. Built ahead of the partner API so its visibility SQL includes suppression from day one. See features.md "Safety suppression".
- [x] (FIXED 2026-09-01) **Featured studios were being subtracted from the leaderboards.** The homepage removed every featured studio id from all three studio boards, so a studio vanished from the Top 100 for the 14 days it held an auto-rotation slot. Surfaced on prod as "Jun Lu Performing Arts has 1,827 awards but isn't ranked" — it was rank #76 and the only studio holding a slot. Invisible locally because no studio there had ever been auto-featured. Rankings are now decided by awards alone; featured studios appear in both the Marquee and their earned rank. Smoke guard + FAQ §14. See features.md "Rankings are objective; featuring is additive".
- [x] (SHIPPED 2026-08-28) **Dancer privacy controls**: per-card Public/Hidden toggle (card editor) + rankings opt-out checkbox (Manage Profile). See features.md "Dancer privacy controls".
- [x] (SHIPPED 2026-08-28) **Community flagging** — user-added content only, auto-dark once then human-decides (griefing guard). See features.md "Community flagging".
- [x] (SHIPPED 2026-08-27) **/partners page** — sponsor/press inbound media-kit + inquiry form (see features.md "Partners page"). Investor strategy: NO public page; TODO below.
- [ ] Draft the private investor one-pager (traction, moat: patent 64/142,611 + 1.5M-award archive + weekly pipeline, revenue ladder: placements -> sponsors -> organizer tiers) — hold in drawer, send on request only.
- [ ] **LAUNCH: September 15, 2026 (confirmed by Q 2026-08-27).** Runway: org invites go out ONE BY ONE starting now, finishing ~Sept 8 (letter: org_invite_draft.md v5; template pre-fills live totals + beta-keyed links; signs "Sam" — confirmed public-facing name). Launch day: lift BETA_MODE. Oct 15: gold-button market opens. Before launch: recruit early_access families for flipbook beta; decide flags beta->on timing.
- [x] (DONE 2026-08-27) **Render-bug defense layers 1+2** (born from the coin-colophon latent 500): `npm run gate` pre-deploy gate = smoke + adversarial-DB sweep (`scripts/sweep_public_pages.js`) + authed route audit; hourly prod sentinel (`utils/sentinel.js`, ENABLE_SENTINEL=true, cron :07) probes stratum-sampled REAL pages (coin-org/acks/photos/collab/rotating-org strata are live queries, so coverage tracks data) — failure-set changes email reviewers + Sentry, 429s skipped, recovery email on clear. Layer 3 = Sentry (existing). Write-path QA stays manual via qa_fixtures.
- [x] (SHIPPED 2026-08-27) **Flip-book card rollout, phase 1 (beta):** prod card_design default -> flipbook (everyone sees the paged back; public back = always-complete certificate); thank_you_notes + award_photos flags -> beta (admins + early_access see photo/ack pages); auto_moderation stays off, moderation_mode manual — all submissions queue at /admin/card-content. Footer now carries "Award card system patent pending" (provisional 64/142,611). Verified live: real dancer page renders 45 flipbook cards w/ tcb pages. NEXT: recruit early_access families → watch queue + mobile layout → flip both flags to 'on' (or set flip_at) when baked.

### Pivot plan (`docs/pivot_plan.md`) — family memory space, decided 2026-09-02
- [x] (SHIPPED 2026-09-02) **P1 — add-first onboarding.** Welcome screen (two buttons, no wall; auto-login for returning devices), guest Add flow (typed dancer name, public event picker, no server session), save gate AFTER the first memory (`app/keep.tsx`, "already saved on this phone" is literally true via the M7 outbox), attach-on-sign-in by folded name with the ambiguity refusal (two same-name household dancers attach nothing). Outbox gained `sendable` policy (guest drafts wait without burning retries) + `attach()` + `waiting` count; 5 new Node tests; typecheck clean, expo-doctor 18/18, iOS export clean. No server changes. See features.md "Pivot P1".
- [ ] **P2 next — the milestone model.** Types beyond competition awards + person creation for kids not in the archive (the parked-draft limit P1 carries). Storage decision (S3 vs R2) comes due here — milestones carry video.
- [ ] **Known pre-existing break, now documented:** `expo export --platform web` fails on expo-sqlite's wasm (`wa-sqlite.wasm` unresolvable) — broken since M7 introduced expo-sqlite, unrelated to P1 (verified identical on the pre-P1 tree). Fix when the web-backup client matters (P6): metro asset config for wasm, or a web-only store shim.

### Mobile app plan (`docs/mobile_app_development_plan.md`)
- [x] (SHIPPED 2026-08-31) **M1 — submission staging, web-first.** Separate `submissions.sqlite` staging file (`SUBMISSIONS_DB_PATH`), `award_submissions` / `award_submission_dancers` / `award_submission_evidence` / household action ledger, `award_provenance` in the canonical DB, `utils/submissions.js` (derive studio from affiliation, require group size, server-side normalisation, idempotency on `(user_id, client_submission_id)`, per-household daily limits), web form at `/manage/dancer/:id/submissions` behind the `family_submissions` flag, and the independent-dancer migration. Smoke covers submit → pending-visible → idempotent-retry → not-public. See features.md.
- [x] (DONE ON PROD 2026-09-02) **Independent migration run on prod.** Dry run matched local exactly, then `--apply`: 451 dancers migrated, 451 synthetic studios created, 918 awards moved, 91 residual rosters flagged, 4 same-name pairs deliberately left. Prod and local now both read 542 independents — parity by identical script runs, no DB copy. Verified: award total unchanged (1,519,425), `dancer_studios` row count unchanged (321,809 — every moved link recreated 1:1, no orphans; the 415 dancers with no studio link are pre-existing and identical in the pre-migration snapshot), no synthetic studio in any leaderboard, synthetic studio URLs 302 to the dancer, dancer pages render `Independent` and never the machine name, homepage studio total 21,785 → 21,694 (−91). Re-run is a clean no-op. Backup at `/opt/awardhome/backups/database.pre-independents-2026-09-02.sqlite` (integrity_check ok).
- [x] (DECIDED + DONE 2026-09-02, local + prod) **3 of the 4 same-name pairs merged.** `scripts/merge_duplicate_dancers.js` (dry-run default, one transaction per merge, refuses unless both records carry the same normalised name, moves all ten `dancer_id` tables — the admin button moves three and isn't transactional). Merged: `Relinda Kozol` 12491→12484, `Diane Doberstein` 37142→37138, `Takdanai McLeod-Smith` 14003→61683. Then re-ran the independent migration on both sides: 3 survivors moved onto synthetic studios, 13 awards moved, independents 542→545, award total unchanged, no orphans, merged-away URLs 404 (never 500). **The evidence pattern is worth reusing:** YAGP publishes one result at several tiers (podium placement + Top 6/12/24) as separate rows, so two records holding *nested tiers of the same event and category* are provably one dancer — a 1st place is inside the top 6. Two pairs were proved that way; Takdanai rested on a globally-unique name plus a valid Senior age progression, and the script records which is which.
- [x] (DECIDED + DONE 2026-09-02, local + prod) **`Zixi Yu` folded — all four same-name pairs are now cleared; the migration reports `SAME-NAME PAIRS : 0` on both sides.** Q confirmed one girl ("registration varies for various reasons") and chose all five records into 37738: the three YAGP rows plus both ADC IBC rows (Cary Ballet Conservatory NC, Princeton Ballet School NJ). End state is one dancer with 11 awards — 9 on `Independent — Zixi Yu (DNC-df62850c-zixi-yu)`, 1 at Cary Ballet, 1 at Princeton Ballet, which is the truth of the record: she competed unaffiliated at YAGP and under schools at ADC IBC. Also retired the `Zixi Yu, NJ` studio (`status='merged'`), a scraper artifact where the results list put her name in the school column.
- [ ] **More dancer-named pseudo-studios exist** (`Deeta Saravanan, NJ` and others). Each is a scraper artifact of an unaffiliated entrant, and each keeps a one-dancer row in the studio table. `merge_duplicate_dancers.js` retires them but only from an explicit hand-written list, deliberately: a name-shaped rule would eventually hit a real studio named after its founder, the same trap `utils/independents.js` avoids by never regexing on "independ". Worth a reviewed sweep before launch — count them first.
- [ ] ~~Urgency note~~ — resolved. The `auto_merge_dancer_profiles` conflation risk that made this pre-`family_submissions` work is gone now that no shared roster carries two same-name dancers. Re-check after any import that adds independent entrants: the migration's collision list is the detector, so read it on every run.
- [ ] **Release `family_submissions`** at `/admin/features` when the reviewer inbox (M3) exists, or to `beta` for early-access families before that. Deploy ≠ release: the code is live and dark.
- [x] (SHIPPED 2026-08-31) **M2 — event candidates and the picker.** `event_candidates` in the staging DB (dedup cluster, geo, promotion state); `utils/eventPicker.js` ranked lookup across organizer tour stops (geocoded, one-tap), other families' candidates, and canonical events by name; server-side dedup-at-creation with a shared cluster when both families insist; `/admin/event-candidates` reviewer queue (superadmin — promotion authority is AwardHome's alone); `scripts/merge_event_candidates.js` auto-merging on unambiguous matches, wired into the end of every successful weekly import. Smoke covers one-tap pick, second-household visibility, dedup offer, cluster, no-canonical-event-written, promote (+ idempotent retry), and auto-merge. See features.md.
- [ ] **Tune the candidate lifecycle constants against real traffic** — visibility 75mi/±14d, dedup 40mi/±3d, auto-merge score 0.75 (env-overridable: `CANDIDATE_VISIBILITY_MILES`, `CANDIDATE_VISIBILITY_DAYS`, `CANDIDATE_DEDUP_MILES`, `CANDIDATE_DEDUP_DAYS`, `CANDIDATE_AUTOMERGE_SCORE`). Picked on reasoning, not data; revisit once families are actually creating events.
- [x] (DECIDED + CUT 2026-08-31, Q) **Event-candidate photos dropped.** Design §6.4 wanted one as dedup evidence ("two candidates with the same banner are the same event"), but that never pays off with the matcher built in M2: two candidates only reach a reviewer side by side when name + date + geography already matched them, so nobody ever compares two banners — it would need perceptual image hashing to add any signal. Against that: an upload path, storage, and a moderation surface for family photographs. Name, date and city carry the identity. Columns removed; design and plan annotated.
- [x] (SHIPPED 2026-08-31) **M3 — studio reviewer inbox + delegated photo approval.** `utils/promotion.js` (solo double-writes primary + junction, groups junction only, find-before-create idempotency, `award_provenance` in the same transaction, refuses to resurrect tombstones, refuses while the event is still a candidate); `/manage/studio/:id/submissions` with confirm / correct-in-place / ask / reject and double-enforced scope; card-photo approval moved from superadmin to the studio, with cast families able to object (one objection routes it to AwardHome). Smoke covers all four plan criteria plus the photo ladder. See features.md.
- [ ] **Superadmin card-photo queue is now exception-only** — `/admin/card-content` still lists everything pending; it should distinguish "objected, needs a decision" from "waiting on a studio", or studios' backlogs will look like AwardHome's work. Small UI change, worth doing before release.
- [ ] **Dancer-level default photos (`dancers.card_photo_*`) still go through superadmin.** They aren't scoped to a routine, so there's no cast to consult. Decide whether the studio should approve those too (the dancer is on their roster) or whether they stay concierge.
- [x] (SHIPPED 2026-09-01) **M4 — convergence, corroboration, the AwardHome queue, and corrections.** `utils/convergence.js` (normalised placement/text keys; absence is not disagreement, so a blank field matches and gets enriched while two different filled values keep awards apart); corroboration by unrelated households (different account AND different dancer) promoting both at `corroborated`; independents auto-approving at `family_submitted` and held out of rankings by `rankableAwardSql()`; `/admin/submissions` catching everything no studio owner would ever see (unclaimed studios, independents, contested); contested dancer claims leaving the studio queue entirely; `award_corrections` field-level proposals with provenance on accept; `scripts/archive_metrics.js` wired into the weekly pipeline. See features.md.
- [x] (SHIPPED 2026-09-01) **M5 — mobile API and auth.** `/api/v1/mobile` mounted after `express.json()` and before session/CSRF/beta-gate (the only place router order carries a security argument); opaque bearer auth with hashes only, rotating refresh tokens, reuse detection that revokes the session, immediate revoke-all; guest-capable reads with cursor + `updated_since`; writes reusing the M1–M4 services; evidence upload grants with magic-byte sniffing and EXIF stripping into a private tree; OpenAPI served at `/api/v1/mobile/openapi.json`; `test/api_mobile.js` (27 checks) against its own throwaway DB, wired in as gate stage 2; route audit extended to sweep the API with a real bearer token. See features.md.
- [ ] **DECISION STILL OPEN — object storage and evidence retention** (design §16.4, plan §9.5). Shipped: the driver interface + a local private-disk implementation, correct at beta scale. Needed before the app widens: S3 vs R2, retention period, and whether evidence is deleted after a decision or kept for the provenance trail. Swapping the driver is three methods; `object_key` is already opaque so stored rows survive it.
- [ ] **Malware scanning is a hook, not an implementation.** `scan_status` stays `pending` and `canServe` treats that as uploader-and-reviewer-only. Wire a real scanner (ClamAV or a service) before evidence is shown to anyone beyond those two groups. Same for image re-encoding (needs `sharp`) and PDF evidence (needs sanitisation) — both deliberately out.
- [x] (DONE 2026-09-01 in M6) **TypeScript client generated from the contract** — `mobile/src/api/schema.ts` via `npm run api:types` (`openapi-typescript` over `docs/openapi_mobile.json`). Nothing in the client restates a response shape by hand.
- [x] (SHIPPED 2026-09-01) **M6 — Expo client: read, recover, claim.** `mobile/` on Expo SDK 57 / RN 0.86, TS strict + `noUncheckedIndexedAccess`, Expo Router typed routes. Guest search and trophy case with no account; email-code sign-in; household dashboard with derived affiliations; profile claiming (including the contested case). Refresh token in `expo-secure-store`, access token memory-only. Single-flight refresh with 7 Node tests. Universal links: `routes/wellknown.js` serves the association files from env, 404 until configured. `npm run mobile:check`. See features.md and `mobile/README.md`.
- [ ] **Move the app to Expo SDK 57** at the first dev-client or TestFlight build, or as soon as App Store Expo Go supports 57 — whichever comes first. Pinned to 56 today only because shipped Expo Go lags npm-latest and refuses a 57 bundle. The cost is one accepted `expo-doctor` failure (Hermes V1 memory regression, fixed only in 57): `npx expo install expo@^57.0.9 --fix`, then use `npm start` not `npm run go`.
- [x] (VERIFIED 2026-09-01) **Runs natively on the iOS Simulator.** `npx expo run:ios` → "Build Succeeded, 0 error(s), 0 warning(s)", installed and launched on iPhone 15, screens render. First genuine native compile; before this only types and a web bundle were checked.
- [x] (SHIPPED 2026-09-01) **M7 — submission, offline, media.** Offline outbox on `expo-sqlite` with exactly-once delivery (id minted at draft creation, never regenerated; 9 Node tests including the lost-acknowledgement case); server-issued event sessions batching a weekend; the Add flow; evidence capture through the M5 grant flow; card photo + thank-you note carried on the submission and copied to the canonical tables at `pending` by promotion; push for decisions and questions only; share sheet plus OpenGraph tags on the dancer page. See features.md.
- [x] (SHIPPED 2026-09-01) **The waiting room made worth waiting in.** A family whose claim is pending can't submit yet, so the trophy case has to earn the wait on its own: awards now come back **most-recent-first** by competition year (they were in import order, which interleaves seasons — a real dancer read 2023, 2025, 2024, 2026), the cursor became an opaque `"<year>:<id>"` keyset, and tapping any award opens the **award card** rather than a database row. When the dancer's studio has no owner the pending panel says so plainly — nobody is reviewing this — and hands her the director's `/claim/studio/<unique_id>` link to share or copy. That link is shown only to a caller with a live claim: which studios are unclaimed is exactly the list a scraper would want. Four contract checks in `test/api_mobile.js`.
- [x] (SHIPPED 2026-09-01) **M8 — a pending claimant can queue.** Staging accepts a household with a live claim; canonical does not. `award_submissions.unverified_household` marks the row, and both reviewer-less doors are shut: independent auto-approval, and corroboration in BOTH directions (an unverified row must not promote the real family it matches — that one is easy to miss). Held out of the studio queue (the claim in front of that director is the same question) and out of `/admin/submissions` (AwardHome cannot judge parentage at all). Approving the claim clears the marker and re-runs promotion over the whole queue; rejecting withdraws it. Standing lives in `householdStanding()` and is consumed by `validateSubmission`, so web and API cannot drift. 4 smoke checks + 4 API checks. See features.md.
- [x] (FIXED 2026-09-01) **Web dancer-claim routing matched the app's.** `routes/dance/claims.js` computed `routeDancerClaim(...)` and threw the result away, filing `studio_id` only when a claim code was supplied — so a codeless claim on the web went to AwardHome while the same claim from the app went to the director. Caught by the M8 release-path smoke test, not by review.
- [ ] **Decide the queue ceiling for unconfirmed households.** The existing per-household daily submission limit applies unchanged, which is probably right (staging is cheap, and a real weekend is a burst). Revisit if a claim that never resolves accumulates an implausible queue.
- [x] (SHIPPED 2026-09-01) **M9 — independents curate; publishing is a separate grant.** Auto-approval existed because no director exists to ask, not because anything was checked — so one weak claim approval granted an unbounded right to publish unreviewed claims about a child. Split: the claim answers "is this your child?", `dancers.independent_publish_status` answers "do we publish your unreviewed entries?". Default `none` = private curation. Corroboration still publishes automatically with no AwardHome involvement (the seed of the contact-based approach). A superadmin grant is retroactive over the whole private record (`releaseIndependentQueue`); revoking is not (unpublishing is a heavier decision). Ask-to-publish on web + app; grant surface on `/admin/submissions`, superadmin-only. Also fixed: a missing `studio_id` was treated as independent, handing 493 unaffiliated dancers the auto-publish door — absence is a data gap, not independence. 6 smoke checks + 4 API checks. See features.md.
- [x] (HARDENED 2026-09-01) **Unverified-household exclusion is now an invariant, not a display filter.** `confirmSubmission` refuses an `unverified_household` row outright, so a reviewer acting on a guessed submission id cannot promote one; the studio's pending-count badge got the same filter it was missing (it counted rows the queue did not show).
- [ ] **Independent publish grants need a revoke surface in the UI.** The route handles `revoke` and the copy is written, but only `approve` has a button — revoking currently means calling the endpoint directly. Add it when the first grant exists.
- [ ] **Corroboration is the only automatic door left for independents.** That is deliberate, but it means an independent at a competition where no other family uses AwardHome stays private until a superadmin looks. Watch the `requested` queue depth once `family_submissions` is released; if it grows, the "friends in other studios" vouching path (contact-based corroboration) is the designed answer, not loosening the grant.
- [x] (FIXED 2026-09-01) **My Dancers made honest.** Pending claims rendered identically to confirmed dancers (M8 put them in `/me`; nothing said which was which), interleaved alphabetically, with an empty state whose copy referenced a list it was replacing. Now: confirmed first then pending newest-first — pinned as an API contract test, since the app renders the order as given — a Pending badge and dashed card, "you can add awards now" on pending rows, section headers only when both kinds exist, and `No dancers yet` meaning no dancer *and* no claim. Screen re-reads on focus, not just mount.
- [x] (SHIPPED 2026-09-01) **The app shows the real award card.** `/dance/card/:dancerUniqueId/:awardId` renders the same `dancer_award_card.ejs` partial the web uses, in a web view — no native reimplementation, so the cqw scaling, per-org coin branding, flipbook back stack and patent-pending design cannot drift, and card changes reach the app without an App Store review. Mounted after session but BEFORE the beta gate (`routes/cardEmbed.js`), the second place router order carries an argument. Scoped per (dancer, award) with 404 for a mismatch. Trophy-case rows now say "View award card ›" — the medal emoji alone did not read as tappable. `react-native-webview` lazily required with a summary+link fallback.
- [x] (DONE 2026-09-01) **iOS dev client rebuilt** — `npx expo run:ios --device "iPhone 15"`, Build Succeeded, installed and relaunched. Verified by symbol, not by assumption: `RNCWebView` (117), `ExpoClipboard` (281), `ExpoNetwork` (40) and `ExpoImagePicker` are all compiled into `AwardHome.debug.dylib`, so the card renders inline, Copy uses the clipboard, and the picker no longer throws the missing-native-module error from M7.
- [x] (FIXED 2026-09-01) **Dev API host is derived from Metro, not pinned.** This machine's DHCP lease moved 192.168.1.243 → .240 mid-session, so the freshly built app pointed at an address that no longer existed — which surfaces as "We couldn't reach AwardHome" and reads like an app bug rather than a stale config line. `resolveBaseUrl()` now takes an explicit `EXPO_PUBLIC_API_BASE_URL` first (what the eas.json profiles set, and the only thing a release build uses), then in `__DEV__` derives `http://<metro-host>:3008`. `.env.local` is deliberately empty. Note `EXPO_PUBLIC_*` is inlined at BUNDLE time and cached — changing it needs `expo start --clear`, not just a reload.
- [x] (FIXED 2026-09-01) **The app opened cards on PRODUCTION while talking to localhost.** Four screens re-read `Constants.expoConfig.extra.apiBaseUrl` themselves for share links and the card web view; when `.env.local` stopped pinning a LAN address they fell back to the production default while `resolveBaseUrl()` correctly derived the local host. Symptom: the private beta gate inside a native sheet, which reads as a server problem, not a client one. `baseUrl` is now exported from `src/api/client.ts` and is the only resolver; `mobile/test/baseurl.test.mjs` fails the build if a second reader or a hardcoded `'https://awardhome.com'` literal reappears.
- [x] (FIXED 2026-09-01) **The beta gate no longer applies in development.** `BETA_MODE=true` gates only when `NODE_ENV=production`, or with an explicit `BETA_MODE_DEV=true` for testing the gate itself. Measured coverage: `/dancer/*` and `/dance/*` are gated; `/login`, `/register`, `/dance/card/*` and `/api/v1/mobile/*` are not.
- [ ] **DECIDE: should public award data leave the beta gate entirely?** Raised 2026-09-01. The gate covers the public data pages, not account creation — the opposite of the intuitive reading. Arguments to open it: award data is public by nature, the org page is already the demo link in invitation letters, and an unfurled share link from the app currently lands a non-beta friend on a password prompt. Arguments to keep it: an unpartnered org's full results becoming browsable pre-launch is exactly what the non-enumerable URLs and the no-org-claim-button decision are protecting against. Not changed unilaterally — it is a launch decision, not a bug.
- [x] (SHIPPED 2026-09-01) **Studio page made decidable.** It showed a name and three counts, then asked "is this your studio?" — undecidable when many studios share a name. Now carries `recentEvents` (newest first; event names carry the city, which identifies a studio better than `address`, on file for only 1,340 of 25,081) and `recentAwards` (named routines first). No dancer names — roster lists stay private, enforced by a contract test. "Already claimed" stopped being a dead end: `is_mine` says so when it's yours, and a signed-out visitor gets a Sign in button returning to the page.
- [x] (SHIPPED 2026-09-01) **Studio claim action pinned; "already claimed" names the manager.** The CTA sat below the new recognition preview, so on a studio with history you had to scroll past two lists to discover you could claim it — now a compact sticky child (off while the form is open). And "someone at the studio manages it" gave no name to ask for: `studio_claims.contact_name`/`contact_role`/`show_publicly` are now structured and consented at collection (the contact name in `proof_text` is verification evidence, not a public byline), promoted to `studios.manager_*` on approval. Existing claimants default to unpublished.
- [x] (SHIPPED 2026-09-01) **Claimant photo on studio claims.** Private, reviewer-only, shown in `/admin/claims` beside the proof text. Reuses the award-evidence primitives (now exported from `utils/evidence.js` rather than re-implemented): magic-byte sniffing, metadata stripping, 0600 outside the served tree. Only attachable to the caller's own pending claim (404 otherwise); a failed photo never fails the claim. Rationale is two-sided — a studio's public "meet the staff" page makes a face checkable in a way a typed name is not, and being asked for one deters speculative claims.
- [ ] **DECIDE: should the manager photo ever be public?** `studios.manager_photo_public` exists and stays 0. A real person's face on a public page is a bigger step than a name and wants the moderation photographs get elsewhere here — not an implicit yes carried along by a claim form. The recognition argument ("families know who to look for in the lobby") is real; so is the objection.
- [ ] **Server-generated share images deferred, with a reason.** They need a headless browser on the production box — real infrastructure, not a detail. Sharing the URL with OpenGraph tags works today and keeps working for someone without the app. Revisit only if link previews prove insufficient.
- [ ] **Push is wired but dark:** `ENABLE_PUSH=true` turns it on, and nothing sends until it is set. Test on a physical device — the Simulator cannot receive push at all.
- [ ] **`expo-image-picker` and `expo-sqlite` are unexercised on hardware.** The outbox logic is tested in Node, but SQLite-on-device, photo permissions and the picker UI are not.
- [ ] **Still unrun: a PHYSICAL device.** The Simulator does not prove Keychain behaviour under a real lock screen, push, camera, or on-device universal links. With the Apple Developer account, `npx expo run:ios --device` is the cheap next step.
- [x] (DONE 2026-09-01) **`IOS_APP_ID` configured** — `XP7TK4Z8M3.com.awardhome.app`, Team ID taken from the signing certificate's OU field (not the parenthesised name on the Development cert, which is a certificate id and would have been wrong). In `.env`; `/.well-known/apple-app-site-association` verified locally.
- [ ] **Universal links cannot be tested until the server is DEPLOYED.** Apple fetches `https://awardhome.com/.well-known/apple-app-site-association` directly over HTTPS with no redirect. It is correct locally and 404s in production until `IOS_APP_ID` is set there too.
- [ ] **Android fingerprints still missing** — `ANDROID_CERT_SHA256` from `eas credentials` after the first Android build. Include the **Play signing key** as well as the upload key; that omission is the classic "links worked in internal, broke in production". `assetlinks.json` 404s until then, deliberately.
- [ ] **TestFlight for the invited cohort:** `eas login` → register `com.awardhome.app` in App Store Connect → `eas build --profile preview --platform ios` → `eas submit`. The `preview` profile already points at production.
- [ ] **App icon and splash screen** — `expo prebuild` warns there is no icon. Cosmetic, but not what you want an invited family's first impression to be.
- [ ] **Point `mobile/app.json` → `extra.apiBaseUrl` at a reachable server** for development (a LAN IP, not `localhost`, when running on a physical device).
- [ ] **CHECKPOINT — decide go/no-go on the mobile client (plan §4, after M4).** M1–M4 are shippable without any mobile client: families can contribute through the web and studios can review. The decision should be made on *observed* submission and review throughput once `family_submissions` is released — not before there is data. Note nothing has been released yet: the flag is still dark.
- [ ] **Metric baselines to watch after release** (`node scripts/archive_metrics.js`): duplicate canonical awards 6,408 and group-awards-with-one-dancer 1,874 as of 2026-09-01. The first must not rise; the second must fall. A rise in either outranks any funnel improvement (design §14).
- [ ] **Corroboration threshold is two households.** Picked from the design, not from data. If two families at one competition turn out to routinely mis-describe the same routine, this is the knob that publishes it — revisit alongside the metric baselines.

### Future (post-launch)
- [x] (SHIPPED 2026-08-28) **Rogue-studio containment, layers 2-3** — see features.md. Original design notes: (design agreed 2026-08-28; layer 1 alert shipped in sentinel): (2) admin Freeze-&-Release tool — provenance-SCOPED: undo source='studio_owner' award_dancers links (attributed today) + dancer_studios rows (needs new `source` column set at every insert site first; families' own dancer_claim links untouched); reuse tombstone machinery. (3) family notification on roster attach ("Not your studio? Remove") — every victim becomes a detector. Thresholds live in sentinel (200 award-links / 100 roster-adds per studio per day) — tune with real traffic.
- [ ] Teacher & choreographer accounts + card credits (two-sided accept; see ideas.md §11). IP: add credit-granularity embodiment to the follow-on provisional queue before building.

### IP / Legal
- [ ] **A11 (owner award weighting) was PUBLICLY DISCLOSED 2026-08-30 by the push+deploy — the "file or consciously waive before deploying" gate was not closed first.** US filing deadline **2027-08-30**; foreign rights largely forfeited. Decide whether A11 goes into P2 rather than letting the deadline decide. Also: `maybe_patentable.md` is in a PUBLIC repo, so it publishes every idea it records the moment it is pushed — treat it as a disclosure channel, not a private drawer.
- [x] (RESOLVED 2026-08-30) **A9 class-mom delegation: Q consciously shipped without filing** — public disclosure date 2026-08-30 recorded in maybe_patentable.md §A9. If filing A9 matter later (e.g. in P2): US deadline 2027-08-30, foreign rights forfeited.
- [x] (DONE 2026-08-29) **AwardHome LLC — Articles of Organization filed** (California, via bizfileonline; entity name matches the brand, so no FBN/dba needed; ComputerMagix sole-prop FBN stays separate, untouched by this project). Terms/Privacy intro + footer copyright now name "AwardHome LLC".
- [ ] **LLC formation follow-ups** (start once SOS approval arrives):
  - [ ] Statement of Information (Form LLC-12, $20) — due within 90 days of filing, so by ~2026-11-27; biennial after.
  - [ ] First $800 franchise tax (FTB Form 3522) — due 15th day of the 4th month after filing (~Dec 15, 2026); Form 568 annually.
  - [ ] EIN (free, irs.gov) → business bank account in the LLC's name; run AwardHome money/contracts through it only (no commingling — the liability shield depends on it).
  - [ ] DMCA designated-agent registration with the Copyright Office under AwardHome LLC ($6, dmca.copyright.gov; list "AwardHome"/"awardhome.com" as alternate names; renew every 3 years). Terms §11/§12 are already DMCA-conformant (agent named as hello@awardhome.com — keep the filing consistent with that).
  - [ ] Assign the provisional (64/142,611) + awardhome.com domain to the LLC at conversion time (fold into the 2027-06-27 checkpoint below).
- [x] (DONE 2026-08-27) **U.S. Provisional Application No. 64/142,611 FILED** (P1+P3 combined: flip-book/multi-face card system + DancerID; spec w/ claims 1-21, FIGS. 1-14, SUPP. FIGS. A-G). "Patent Pending" now usable in marketing/outreach. Freeze list LIFTED (flip-book rollout, demos, outreach OK). See provisional_patent_plan.md banner.
- [ ] **On the SECOND filing (P2 or sponsor-page follow-on): flip footers to "Patents pending"** (plural becomes true then; today's unscoped singular "Patent pending" is deliberate — maximal legitimate ambiguity, provisionals are unpublished so scope is unknowable to copycats; NEVER mark plural/scope beyond actual filings — 35 USC 292).
- [ ] **2027-06-27 checkpoint (calendar it!):** convert / PCT / refile decision. HARD deadline 2027-08-27 (non-extendable).
- [ ] P2 provisional (staged-import pipeline A1/A2) — self-draft from docs/db_operations.md per plan §4; decide by the month-10 checkpoint.
- [ ] Post-filing features beyond the spec need follow-on provisionals before public ship — check maybe_patentable.md before releasing anything novel. **Queue so far: sponsor credit page ("made possible by X" — NOT in 64/142,611; see plan's "New matter" section). Bundle with P2 filing or file standalone before the feature ships.**

### Data quality
- [x] (DONE 2026-08-31 on PROD) **Spotlight title-winner repair applied live** — import (+244) then repair (-70 corrupted rows, -69 fused profiles, -2 phantom studios); title winners 730 → 880. Verified idempotent. Original note:, as `sudo -u awardhome`: (1) `node scripts/import_spotlight_pdfs.js` (inserts the missing title winners; the PDFs are already on prod), (2) `node scripts/repair_spotlight_title_rows.js` — review the dry run, then `--apply`. Both idempotent. Expect ~+224 awards and ~70 corrupted rows removed.
- [x] (DONE 2026-08-30, local) **Spotlight title winners: real winners were never imported — fixed and repaired.** `import_spotlight_pdfs.js` matched placements against a list lacking both "Miss"/"Mr" (how Spotlight records the ACTUAL title winner — so every winner row was silently skipped) and Spotlight's "1st Run Up" / "1st Runner-Up" spellings of "1st Runner Up" (the regex fell through to bare "1st", leaving the placement tail as the first token, which the Title-Winner branch took as the STUDIO — giving phantom studios "Run Up"/"Runner-Up", dancer names with the real studio fused on the front, and a RUNNER-UP recorded as place "1st", i.e. a first-place win). Pattern generalised to match the shape (`\d(st|nd|rd|th)\s+Run(ner)?[-\s]?Up`) rather than enumerating spellings; Miss/Mr normalised to WINNER. Unit-tested over 12 placement forms incl. regressions. Repaired: re-ran the importer (+224 rows, **title winners 730 → 880**), then `scripts/repair_spotlight_title_rows.js` removed the 70 superseded corrupted rows, 69 fused dancer profiles and both phantom studios — only ever deleting a row whose corrected replacement is already present. Verified against source: GEG 2022 Junior title now reads WINNER/Dance Images West/Scarlett Stemp + three correctly-labelled runner-ups. 0 phantom studios, 0 mislabelled runner-ups remain.
- [x] (DONE 2026-08-31, local) **Ultra: 90 dancers were named "<Name> - <Level & Age>".** NOT a scraper misread — Ultra's own scholarship column header reads `Dancer - Level & Age` and the cell genuinely contains "Isabel Jones - Competitive Plus 8". Our bug: `scrape_ultra_to_txt.js` matched the header on a SUBSTRING, so that variant was read as a plain `Dancer` column and the level+age became part of the person's name. Scraper now splits on the header's own contract and keeps the level/age as the category. `scripts/repair_ultra_dancer_levels.js` merged 86 into their existing real profiles, renamed 3, and correctly left "Ariel Lantz - Loza" alone (a name, not a division — only level vocabulary is stripped).
- [x] (DONE 2026-08-31, extractor) **StarQuest extractor hardened — 5 defects fixed.** (1) Field separator was `col.text === '-'`, missing en/em dash, figure dash and minus — now any lone dash glyph. (2) Many rows carry NO separator and are laid out by position only; added GAP-based splitting, threshold 0.25 calibrated against the PDFs (within-field fragments measure ≤0.17, real boundaries ≥0.34 — an empty band between them), using span `x + w*0.75`. NOTE `w` had to be captured; the extractor stored only `{x,text}`, so spans collapsed to their start x. (3) Removed the self-described "very crude fallback" that assigned the WHOLE row string as a dancer name (source of ~186 fabricated people); an unsplittable row is now counted and surfaced, never invented. (4) `isSpecial` missed "Viral Video of the Event" — now decided by whether the category names a routine size, and tested BEFORE the ranked-placement branch since costume/photogenic/video tables are never ranked. Choreography Awards mapped as Studio-Choreographer (they honour a person, not a routine). (5) Fragments are now joined RAW, using the document's own trailing-tab word breaks, which also removes the tab-laden field values ("Brooklyn\tSt.\tAubin") and mid-word damage ("Pilato Danc e Center" → "Pilato Dance Center"). Result: 41,372 → 42,093 award rows (+721 previously dropped), fused dancer fields 93 → 0, mid-word damage 7 → 0, 0 unsplittable rows. Previously-correct rows verified byte-identical apart from tab normalisation.
- [x] (DONE 2026-08-31, local) **Two latent normalization gaps found while attempting the StarQuest re-import — both fixed and applied.** (a) `scripts/normalize_studio_whitespace.js` — studios were the ONLY entity never whitespace-normalized (dancers and routine titles were done in an earlier pass). 1,648 damaged names, of which **895 were outright duplicates** of an existing clean-named studio, splitting a studio's awards and roster across two rows. Applied: 928 merged, 720 renamed, 0 claimed studios touched. Discovers its 16 studio-referencing columns from the schema rather than a hand list (which missed `awards.merged_from_studio_id` and `studio_merge_requests.target/source_studio_id`), and never DELETEs from `awards` — merged_from_studio_id is provenance, not ownership. (b) `scripts/normalize_award_field_whitespace.js` — category/award_type/place/**notes** are the importers' idempotency key. 38,742 damaged values meant a fixed extractor's clean text matched NOTHING, so a re-import inserted a full duplicate set (21,732 phantom awards on the first attempt, rolled back from a snapshot). `notes` matters because importers stash the dancer name there and dedupe with `notes LIKE '%name%'`.
- [x] (DONE 2026-08-31, local) **Studio alias resolution — 3,386 duplicate studios merged, and the cause fixed at import time.** Q also asked for case normalization; both case and spacing are covered. `scripts/merge_studio_aliases.js --tier=case|space|punct` (default case, the safe one) merges variant groups; applied all three: 1,193 case + 625 spacing + 472 punctuation. Survivor = the claimed studio, else most awards, else best-cased name; groups with TWO claimed studios are reported, never merged; a human's earlier `rejected_merges` decision is honoured. **SOFT merge** matching utils/studioMerge.js (status='merged' + merged_into_id) so the loser's public URL redirects instead of 404ing and importers resolve through it — my first version hard-DELETED, which broke both. Merge CHAINS are flattened to the terminal studio, because both readers of merged_into_id follow exactly one hop. Zero claimed studios dissolved, zero chains, awards unchanged at 1,520,091.
- [x] (DONE 2026-08-31) **`utils/resolveStudio.js` — the durable fix.** Importers matched studio names exact + case-SENSITIVE, so any corrected extractor minted a duplicate studio per variant. The shared resolver walks exact → whitespace → case → spacing → punctuation (widest last), follows merged_into_id to the live survivor, and reports which tier matched. Wired into `import_starquest_txt.js`; the other importers should adopt it. Measured on a StarQuest replay: 3,137 lookups resolved that would each have created a duplicate studio.
- [ ] **StarQuest data replay: now unblocked, needs a reconciler.** With the studio work in place a replay inserts 3,206 rows — 2,937 of them the CORRECTED Choreography/Costume/Viral-Video rows and only 81 residual studio dupes (was 21,732 at the first attempt). The superseded corrupted rows still need removing, exactly like `scripts/repair_spotlight_title_rows.js`: delete a corrupted row only when its corrected replacement is already present. Import was rolled back; DB is at 42,851 StarQuest awards with the old rows intact.
- [ ] **AUDIT 2026-08-30 — 1,924 awards whose routine name is purely numeric** (NexStar 474, KAR 192, Starpower 150, Revolution 148, Rainbow 137). Likely entry numbers landing in the routine column. Needs per-org source checks before any repair.
- [ ] **REVIEW QUEUE: 109 awards where several linked dancers genuinely share the award's studio** (mostly KAR — e.g. "Paquita", "Fairy Doll" ballet variations with 3 linked dancers). The source cannot disambiguate these by studio, so both repair scripts report and skip them. `node scripts/repair_collapsed_solo_dancers.js` lists the live set. Needs a decision on how to resolve before building tooling.
- [x] (DONE 2026-08-31 on PROD) **Parser-artifact repairs applied live** — Showstopper apostrophe (80 fakes, 5 studios), collapsed solos (191 resolved + 141 reattached), solo-primary backfill (355). All verified idempotent. Original note:, in order, as `sudo -u awardhome`: (1) ship the re-extracted Showstopper txt (`tobeprocessed/pdf/showstopper/txt/`, remember to delete macOS `._*` files after untarring), (2) `node scripts/repair_showstopper_apostrophe.js` then `--apply`, (3) `node scripts/repair_collapsed_solo_dancers.js` then `--apply`, (4) `node scripts/backfill_solo_primary_dancer.js --apply`. All idempotent; review each dry run first.
- [x] (DONE 2026-08-30, local) **Solos were showing under "Group Dancers" — 79,181 awards had no primary dancer.** Q spotted it on /manage/studio/842/awards. Cause was NOT the routine-editing/sync flow he suspected: only 6 of 67,574 rows carried source='studio_owner' — five importers (Showstopper, Starpower, NYCDA, Revolution, and the shared DanceBug importer behind Imagine/Believe/DreamMaker/Rainbow) wrote the `award_dancers` junction but never the legacy `awards.dancer_id`, and `backfill_legacy_dancer_links.js` only ever ran the OTHER direction. Not just cosmetic: public queries joining `a.dancer_id` (Hall of Fame, card surfaces) rendered a blank dancer — solo/title awards with a linked dancer but blank name went **79,538 → 357**. Built `utils/soloPrimary.js` (one shared definition), `scripts/backfill_solo_primary_dancer.js` (applied: 79,181), fixed all five importers to double-write, wired the sweep into the weekly pipeline, and made the awards editor defensive (renders a solo's single linked dancer as Primary, marked "(linked)"). Safety rule: identification is POSITIVE (label says solo/title, no duo/trio/group/line wording) and NEVER inferred from "one linked dancer" — 1,874 group-worded awards have exactly one link and promoting those would turn groups into solos. Verified: 0 group awards gained a primary. See docs/db_operations.md + features.md §2b-i.
- [x] (DONE 2026-08-30, local) **The 232 multi-dancer "solos" traced to source — TWO unrelated parser bugs.** (A) `import_dancebug_awards.js` deduped Choice awards on (event, category, routine, place), omitting studio_id and performance_number — so two studios' same-titled routines collapsed into one award (Believe Louisville 2022: entry 259 Aubrey Sears/Dance Designs vs entry 330 Laney Wheeler/All About Dance), and `backfill_utils.js` propagated the bad pair because (routine, studio) is not unique for a performance. 191 repaired by the dancer's own studio + 141 displaced dancers REATTACHED to their own empty placements. (B) `extract_showstopper_pdfs.js` — a typographic apostrophe in a studio name decodes as control char U+0019, pdf2json emits it as its own column, every column shifts, and the parser took dancers positionally: the studio was truncated ("Chassé") and the studio tail, STATE CODE and SCORE became dancer names. **5 phantom studios and 80 fake dancer profiles (71 with public URLs)** — all removed, studios renamed/merged, 81 txt lines re-extracted correctly. All four fixes are at the source, so neither recurs. See docs/db_operations.md.
- [x] (DONE 2026-08-31 on PROD) **Solo primary-dancer backfill applied live** (79,163 rows); reconciler reports zero. Original note: (prod has the same 79k): `sudo -u awardhome node scripts/backfill_solo_primary_dancer.js` (review dry run), then `--apply`. Idempotent; the weekly pipeline maintains it thereafter.
- [x] (DONE 2026-08-30, local) **NexStar PDF extraction defect — 21% of the org was mis-labelled.** Q spotted it on the April 12 2025 San Jose event: everything printed after "SDA Regional Champion - Level 3 12 & Over Grand Lines" (Artistic Excellence Award, Battle on the Seas, …) had been filed *under* that champion heading. Cause: `extract_nexstar_pdfs.js` recognised a table header only if it began with "Place", but NexStar's named-special tables have no place column ("Routine Name | Studio") — so their header parsed as a data row AND the section title never advanced, leaving every winner beneath tagged with the previous section. Fixed the extractor (both header layouts + the 2026 "Routine Name | Routine Name" duplicate-label export), re-extracted all 306 PDFs, and repaired the DB with the new `scripts/reconcile_nexstar_awards.js` (corrects award_type **in place** so award ids and dancer links survive; refuses to bulk-delete past a safety limit). Result: 25,981 award types corrected, 714 artifact rows removed (0 carried user data), 3,873 recovered awards inserted, 114 award types that had never existed in the DB now present; reconciler reports a clean idempotent state. Re-encoded NexStar tiers, which also exposed that the T2 rule matched the word "level" and so missed 13,785 division placements (36%) named by skill word/brand tier instead — now matched by shape. NexStar majors 31,726 → 48,410. See docs/major_award_policy.md §11.
- [x] (DECIDED 2026-08-30) **NexStar choreography removed from top awards.** Shown the volume (~19/event vs the ~1/event the ruling assumed, with the identical sibling *Excellence in Entertainment* uncounted), Q: "nexstar choreography is not as significant then… let's remove that from top_award." Implemented as `skipOrgs: ['nexstar']` on the common rule — NOT an UNFLAG in NexStar's own array, because common rules run last and would re-flag it. NexStar majors 49,537 → 46,503 (T3 4,737 → 1,703); all other orgs untouched (11,039 choreography awards still count). Applied LOCAL. **Prod still counts them (46,503 vs prod's 49,537)** until this commit is deployed and `node scripts/encode_top_awards.js --org=nexstar --apply` is re-run there as `sudo -u awardhome`.
- [x] (DONE 2026-08-30 on PROD) **NexStar repair applied live** — shipped the corrected txt corpus (old corpus kept at `tobeprocessed/pdf/nexstar/txt.pre-fix-2026-08-30/`, DB snapshot at `/opt/awardhome/backups/database.pre-nexstar-fix-2026-08-30.sqlite`), then reconcile (25,962 types corrected, 714 artifact rows removed, 0 protected) → import (3,873 recovered awards, 13 new studios) → encode → header-artifact purge (8 rows across 24SEVEN/RADIX/Imagine/Spotlight). Reconciler now reports all-zero on prod. **Exact local/prod parity**: 126,273 NexStar awards, 49,537 flagged, T1 6,270 / T2 38,530 / T3 4,737, 1,368 distinct types. Site healthy; the reported April 12 2025 event verified live. NOTE: macOS `tar` injects `._*` AppleDouble files — they parse as empty txt files and must be deleted after extracting on prod (`find . -name '._*' -delete`).
- [x] (DONE 2026-08-31, PROD + local) **Dancer convergence catch-up applied both sides.** normalize_dancer_whitespace: 0 pending (already clean). auto_merge_dancer_profiles: **prod 2,939 duplicate profiles merged across 2,870 dancers; local 2,972 across 2,902** — higher than the 1,070 originally estimated because the studio alias merges put many more same-name dancers onto a shared roster, which is exactly the signal this script keys on. 0 components with multiple claimed profiles, 0 protected dups. Both idempotent afterwards. Original note: (1) `sudo -u awardhome node scripts/normalize_dancer_whitespace.js --apply`, (2) `sudo -u awardhome node scripts/auto_merge_dancer_profiles.js --apply`** (one-time catch-up; weekly pipeline maintains both thereafter). Local: 194 names cleaned, 1,070 dup profiles merged.
- [x] (DONE 2026-08-31, PROD + local) **StarQuest notes-dancers + legacy link backfill.** repair_starquest_notes_dancers: 3 rows each side (the 18k bulk had already been done). backfill_legacy_dancer_links: 0 pending — already converged. Original note: (1) `node scripts/repair_starquest_notes_dancers.js --apply` (StarQuest importer stashed 18k dancer names in notes without linking — see docs/db_operations.md), then (2) `node scripts/backfill_legacy_dancer_links.js --apply`** (one-time catch-up; weekly pipeline handles it thereafter). Local run promoted 264k solo links. Skipped-for-cause remainder (~5.4k local): mostly same-name duplicate dancer profiles (one legacy-linked, one junction-linked — merge the profiles via the roster tools, which now repoint awards.dancer_id, and the next weekly backfill converges them) plus ~51 stale pointers to dancers deleted by pre-fix merges (mapping lost; harmless, skipped forever).
- [x] (DONE 2026-08-28) **Upcoming directory: 2026-27 tours for all 10 result-orgs added this week** — 230 events (Tremaine 22, Inspire 50, TDA 2, JUMP 27 / NUVO 26 / RADIX 23 / 24SEVEN 26 via tour-dates season-container, Hollywood Vibe 21, ADC|IBC 18 incl. Berlin+Toronto, UBC 16 read via browser past Cloudflare). All geocoded (intl centroids hand-cached, key format city|st); free gold on each org's opener; local+prod parity 1,056 active / 26 golds. Importer hardened: seed lines for the 10 weekly-scraper orgs now SKIPPED (stale-seed re-import had resurrected 56 superseded rows → duplicates; cleaned). Watch: UBC 2027 Grand Prix Finals + ADC "Central Florida (Pending)" not yet announced — add when posted.

- [ ] Hall of Fame Dance Challenge (halloffamedance.com, east-US): NO public results — researched 2026-08-27. Custom platform (not DanceBug/CompGenie); /CompetitionResults/<id> exists but returns Invalid Request without a studio login; Wayback has zero results-URL captures ever; studios post their own results (e.g. parkfun.com Schaumburg news page). Data only obtainable via partnership ask — good outreach candidate BECAUSE families can't find results anywhere public.
- [x] (DONE 2026-08-27, imported local+prod: 134 events / 19,039 awards / 6,673 firsts / 480 new studios / ~21.9k new dancers / 86,108 award_dancers links; parity exact; idempotent; smoke green; stats ritual done — org #25, platform at 1,487,944 awards) **Inspire NDC import.** Follow-ups: 2022 + 11 unposted events exist only via partnership ask; watch for new season PDFs (scrape script is incremental); 2019-2022 not in the public portal. Downloaded all public results from their CompGenie portal (scripts/scrape_inspirendc_results.js: result.aspx postback flow → raw/inspirendc/<year>/, 134 files 2023–2026; 2022 posted nothing; 11 events unposted). Extracted via scripts/extract_inspirendc.py (tag-based tables through new shared scripts/lib/pdf_tags.py, factored out of Hollywood Vibe with byte-identical regression) → tobeprocessed/inspirendc/txt/: 134 events / 19,045 rows / 22 FLAGGED / 93 CHECK-notes. Sections: OVERALL 15k (with FULL group rosters from the Performers column — first org with complete group dancer names), TOPSCORE, TITLE-MISS/MR/NB, SHOWCASE (Nationals Crystal Showcase), PHOTOGENIC/COSTUME/STUDIO extras. txt REVIEWED by Q; scripts/import_inspirendc_txt.js written + dry-run 2026-08-27: 134 events / 19,045 awards (6,679 firsts) / 480 new studios / ~21.9k new dancers / 86k award_dancers junction links (groups via junction, solos double-write dancer_id). Photogenic winners (no studio in books) matched against event rosters per Q's instruction (410 local + 69 unique-global + ~239 honest studio-less profiles — 2023 books only print placing routines). Idempotency REHEARSED on VACUUM copy: apply → re-apply = 0 inserts. REMAINING: apply local, scp txt tarball to prod (artifact-of-record convention), apply there, re-pull platform stats (landing counts, invites, marquee — Inspire will be org #25). 2024 + two 2023 events are Title-only (org only posted the CompGenie title report those seasons).
- [x] (DONE 2026-08-28, imported local+prod: 79 events / 19,681 awards / 5,944 firsts / 182 new studios / 41 DOTY dancer links; parity exact; idempotent; smoke green; stats ritual done — org #26, platform crossed 1.5M at 1,507,625 awards) **Tremaine import (2022–2026).** Backfill option: 2018–2021 seasons already extracted (16 events + 28 DOTY seasons) — import via --from=2018 when wanted. Original discovery notes below.
- [x] **Tremaine txt extraction** (2026-08-28). Downloaded 91 winners PDFs (2018–2026) via scripts/download_tremaine_pdfs.js — live site links only the current season, but Wayback captures of the winners pages revealed the stable filename convention back to 2018 (seed: scripts/seed/tremaine_pdf_urls.txt); de-linked files often still live on the server, rest recovered from Wayback captures. Extracted via scripts/extract_tremaine.py → tobeprocessed/tremaine/txt/: 90 events / 22,558 rows / 0 flagged (PLACEMENT 17.2k, QUALIFIER 2.3k incl. '**'-starred, HIGH SCORE 1.4k, JUDGES' OVATION 1.1k, SHOWMANSHIP, NF/NAT finals medals, Faculty Show Invitee, I.D.A. Winner). Plus scripts/extract_tremaine_doty.py: 203 Dancer of the Year titles across 33 seasons (1993-94 → 2025-26; current season has studios). Dancer names otherwise appear only for independents. UNRECOVERABLE: 49 events (mostly COVID-era 2020-21 + 2021-22 + 2022-23 winter tours) — deleted from the server and never crawled by Wayback (only post-deletion 404 captures); partnership ask could fill them. Next: review txt → write import_tremaine_txt.js (DOTY events need a per-season pseudo-event decision) → local/prod apply.
- [x] (DONE 2026-08-28, imported local+prod: 27 events / 8,475 awards / 535 firsts / 175 new studios / 4,642 dancer links, 3,311 matched cross-org; parity exact; idempotent; smoke green; stats ritual done — org #27, platform at 1,516,100 awards) **The Dance Awards import (full 2011–2026).** Refresh after each summer finale: scrape_thedanceawards_to_txt.js --refresh → import --apply. Discovery notes: Break The Floor's season finale (sibling of our JUMP/NUVO/24SEVEN imports) publishes its ENTIRE winners history as static JSON behind the Angular SPA — no scraping heuristics needed. scripts/scrape_thedanceawards_to_txt.js caches content/json/{all-results,winners,hall-of-fame}.json in raw/thedanceawards/ → tobeprocessed/thedanceawards/txt/: 27 events (Orlando+Las Vegas, earlier New York) 2011–2026, 8,475 rows, 0 flagged. 4,642 rows carry dancer NAMES: Best Dancer titles (3,897 — winner/2nd/3rd + Top-N finalist tiers, with gender) and Class Scholarships (505). Also SPECIALTY (real award names + genre; choreographer/director in notes), BEST-PERFORMANCE, HIGH-SCORE-AGE/PERF, STUDIO-OF-THE-YEAR. hall-of-fame.json (224 Best Dancer winners WITH PHOTOS) cached for a future photo enhancement. Next: review txt → importer (decide year scope; 2022–2026 would match recent convention, but full 2011+ history is clean).
- [ ] Headliners Dance Competition (headlinersdance.com, CompGenie): NO public results — researched 2026-08-26. Their CompGenie portal DOES expose the public results module (headliners.dancecompgenie.com/Dance-Competition/result.aspx, Year 2010–2027 + Month/Region/State filters), but the grid returns "There is no data" for every year/filter combination tested — the org opts out of public posting; results/score sheets go to studio logins + the Dakiki app. WP site: zero results URLs in sitemaps (161 URLs) and only one ever in Wayback's 1,197 captured URLs (/2021-results-morristown/, already deleted when first archived 2022). Outreach-only, same pitch as Hall of Fame: families can't find results anywhere public. NOTE for future CompGenie orgs: result.aspx postback flow works (year ddl __doPostBack → btnSearch) — the module is scrapeable when an org populates it (e.g. inspirendc.dancecompgenie.com/Dance-Competition/result.aspx).

- **Convention (2026-08-27): reviewed txt is the artifact of record.** For org imports, transfer tobeprocessed/<org>/txt/ to prod (scp tarball) and import there — never re-scrape/re-extract on prod (divergence window, double site hits). NUVO was the last org imported via prod re-scrape (verified identical: 138 events / 71,419 rows both sides).

- [x] (DONE 2026-08-25) Ultra + Refresh joined the Monday weekly pipeline: WEB_ORGS entries run scripts/update_txt_org.js (scrape year → txt → import --apply into DB_PATH), so new results flow through the staged validator gate like every other org. Verified against a snapshot: cache-hit scrape + zero-row idempotent import.
- [x] (DONE 2026-08-25) Refresh Dance Competition import: new KAR-family org (started 2026), scraped refreshdance.com/competition/results/2026 via scripts/scrape_refresh_to_txt.js (adapted from Ultra; same DanceBug platform) — 9 events / 2,644 awards imported identically local+prod via import_refresh_txt.js; 9 new studios, ~256 new dancers; idempotent.
- [x] (DONE 2026-08-25, imported local+prod: 91 events / 31,365 awards / 4,465 firsts / 98 new studios / ~2.76k new dancers; idempotent; parity verified) Ultra Dance Tour import: KAR-family org (like Rainbow); scripts/scrape_ultra_to_txt.js scraped ultradancetour.com/competition/results/2022..2026 → 91 events / 31,408 rows (16,232 with dancer names) in tobeprocessed/ultra/txt/ for review; pages cached in raw/ultra/. Import via scripts/import_ultra_txt.js --apply (dry-run verified: 91 events, 98 new studios; creates dancers by name+studio for solos). Play Video stripped at scrape.
- [x] (DONE 2026-08-25) NexStar PDF import: 305 of 306 queued PDFs (2021-2026) parsed via scripts/extract_nexstar_pdfs.js (handles 4 format generations: positional tables, stroke-doubled headers, +29-shifted font encoding, single-fragment text-mode w/ lazy retry) and imported via import_nexstar_txt.js — 305 events / 123,114 awards / 25,021 firsts, 2,569 new studios, idempotent (re-run inserts 0). Data confirms titles ARE branded "Miss/Mr Nexstar" (docs/org_top_awards.md updated). Held: 2021-june-26-july-02 (partial font cipher, 1 file); 453 rows imported with NULL studio.
- [x] (DONE 2026-08-25) "Play Video" title contamination: the KAR/Rainbow results site nests a "Play Video" link in the routine cell and .text() concatenated it into 173,210 titles (125,465 KAR + 47,745 Rainbow — no clean twins, pure rename). Scrapers fixed (scrape_kar_year/scrape_rainbow/scrape_dancekar strip the suffix), cleanup via scripts/fix_play_video_titles.js (dry-run default, --apply; run identically on local + prod). Root cause of the 2026-08-24 RED import review (suffixed refetch != clean prior row -> idempotency break); dismiss that held import — the next weekly run is clean.
- [ ] Review the 553 flagged same-name-no-evidence studios (needs_investigation=1, still tab-named) — possibly distinct studios sharing a name; merge or clean case-by-case.
- [ ] Clean-vs-clean duplicate studios: 892 same-normalized-name groups with no tab member (case/punctuation variants like "ELITE DANCE ACADEMY" vs "Elite Dance Academy") — needs the same evidence-based approach.
- [ ] Studio+teacher concat records from the StarQuest import (e.g. "Lanzi Academy Of Dance Christina Lanzi" #16125) — detect and merge into the base studio.
- [x] (DONE 2026-08-09 via scripts/backfill_showstopper_dancers.js, applied local+prod identically: 241 awards completed, 2,413 dancer links added — 2,227 matched existing rosters, 192 created; split names reconstructed, lone-word fragments never become dancers.) Showstopper dancer backfill: awards had fewer linked dancers than the re-extracted txt lists (overflow name lines were swallowed by the old extractor bug).
- [x] (FIXED 2026-05-25, 4ba2911: all importers now match dancers by name+studio, creating separate records per studio; fix_duplicate_dancers.js split the pre-existing mixed unclaimed records — verified 2026-08-10: "Angela Zhang" is 10 clean per-studio records.) Dancer same-name mixing bug (duplicate_name_bugs.md is stale).
- [x] (DONE 2026-08-25) Encore import, studio-bearing events: 63 events / 24,988 awards / 5,165 firsts imported identically on local + prod via scripts/import_encore_txt.js --apply (115 new studios; idempotent re-run verified). The 8 studio-less events (2022 era + 2023-feb + 2023-oct, 3,278 rows) are HELD in tobeprocessed/pdf/oncore/txt/held/ pending studio attribution from Encore (entry numbers preserved in performance_number for a future entry→studio join). Ask Encore for 2022 attribution as part of outreach.
- [ ] Admin dancer merge/split tools — the remaining half of the same-name work: merge records for a real dancer who trains at / moved between studios (split records are now the default), split any claimed profiles the 2026-05-25 cleanup skipped.

### Security / launch
- [x] (DONE 2026-08-19) Anti-scrape rate limit: per-IP limiter (PROFILE_RATE_LIMIT, default 100/5min, admins exempt) on the five enumerable surfaces — /dance/studio/:id, /dance/studio/:id/first-places, /api/studio/:id/year/:year, /dancer/:unique_id, /widget/studio/:id; smoke suite bursts a profile to verify the 429.
- [ ] Anti-scrape follow-up: consider Cloudflare bot-fight / managed challenge rules on top of the app-level limiter.
- [ ] Launch-day SEO: sitemap.xml for studio/dancer pages (replaces the public directory as the crawl path once BETA_MODE=false).

### Features
- [x] (DONE 2026-08-27, imported local+prod: 128 events / 76,369 awards / 6,124 dancers / 155 studios; idempotent) 24SEVEN Dance Convention import, 2022-2026 — fourth DanceOne brand, pure config over the shared libs. Youngest division "Sidekick"; slug 'twentyfourseven'. Prod via reviewed-txt scp (the convention). 56,575 dancer-named rows. All four DanceOne brand indexes verified mutually disjoint.

- [x] (DONE 2026-08-27, imported local+prod: 114 events / 62,234 awards / 6,615 dancers / 149 studios; idempotent) RADIX Dance Convention import, 2022-2026 — third DanceOne brand, pure config over the shared libs (scrape_radix_to_txt.js + import_radix_txt.js, ~12 lines each). Youngest division "Rookie". Brand indexes verified disjoint (the shared backend serves any id from any brand site — only own-index ids are fetched). First org shipped to prod via the reviewed-txt scp convention (no prod re-scrape). 45,061 dancer-named rows.

- [x] (DONE 2026-08-27, imported local+prod: 138 events / 71,415 awards / 8,918 dancers / 330 studios; idempotent) NUVO Dance Convention import, 2022-2026 — DanceOne sibling of JUMP with identical page anatomy, so the JUMP scraper/importer were refactored into shared libs (scripts/lib/danceone.js + danceone_import.js; per-org scripts are thin configs; JUMP regression-checked byte-identical txt + 0-insert re-import). NUVO adds the NUbie age division, BALLROOM class scholarships, and a Faculty column on class-scholarship tables (ignored). 51,412 dancer-named rows. 24SEVEN (also DanceOne) would now be a ~10-line config if wanted.

- [x] (DONE 2026-08-27, imported local+prod: 143 events / 82,520 awards / 20,904 dancers / 658 studios; idempotent re-run 0) JUMP Dance Convention import, 2022-2026: scripts/scrape_jump_to_txt.js parses real HTML tables (Cheerio, pages cached under raw/jump/) — event index from /past-seasons/ (all seasons in one page; ?season= is ignored server-side), results from /results/?id=N with four table shapes told apart by their <th> row. 62,302 rows carry dancer names (solos name the dancer in parens; scholarships are dancer-level). Archive reaches 2019-2020 if we want more history.

- [x] (DONE 2026-08-27, imported local+prod: 96 events / 46,726 awards / 7,283 dancers / 1,153 studios; idempotent re-run 0) Hollywood Vibe import — extraction DONE 2026-08-27 (awaiting Q's review of tobeprocessed/hollywoodvibe/txt/): scripts/extract_hollywoodvibe.py parses 96 events / 46,734 rows from the 2022-2026 PDFs with 280 flagged lines (0.6%). TAG-BASED, not positional: these are Word exports carrying a real /Table -> /TR -> /TD structure tree, so rows and cells are read from the PDF's own markup and nothing depends on column x-positions (a first positional attempt flagged 2,295 lines; tags cut that to 280). Cell text decodes through each font's /ToUnicode CMap — several events embed subset fonts as hex strings that are otherwise invisible. Handles both routine/studio conventions (dash and quoted-title), 1st-5th plus the 6th-10th continuation tables, OVERALL and SPECIALTY award tables, and the scholarship/agency-finalist lists (dancer names!). NEXT: review txt, then build the importer (org row + events by location/year; note ~10,800 first places).

- [x] (DONE 2026-08-27, imported local+prod: 5 events / 537 awards / 343 new dancers / 81 new studios / 109 ensemble links; idempotent re-run 0; 16 non-dancer specials deliberately skipped, Outstanding School imported as studio award) ADC|IBC import — extraction DONE 2026-08-27: scripts/extract_adcibc.js parses the Wix winners pages (2022-2026) → 552 awards, 0 flags, in tobeprocessed/adcibc/txt/ awaiting Q's review (gender attribution via header-queue + GOLD-MEDAL run detection; 2022-25 Primary was combined, 2026 split F/M; ensembles paired routine+dancers+studio). NEXT after review: create org row + importer (dancer-centric like solos, award_dancers for ensembles), then outreach — site only publishes World Finals, so the ask is their regional/semi-final results (they run ~10 semi-final cities/season). Site archives reach 2006 if we ever want deeper history.
- [x] (DONE 2026-08-27, imported local+prod: 65 events / 11,625 awards / 5,771 dancers / 489 studios / 2,295 group links; idempotent) UBC (Universal Ballet Competition) import, 2022-2026: extract_ubc.js reads the registration backend (results.cfm?event_id=N — the marketing site is Cloudflare-gated, the API is not; season_id is ignored server-side, so event ids alone enumerate the archive). Dates/cities come from scripts/seed/ubc_events.json, built once from the live site + Wayback snapshots of past-season index pages (their ?id=N links); all 81 ids cross-checked against each results page's own city header, 81/81 matched. Parser handles 4 row shapes (solo / duo-trio / ensemble / Additional Awards). UBC publishes regionals AND finals — no outreach ask needed for regional data (unlike ADC). Deeper archive: ids 8-28 cover 2019-2021 (~1,100 more placements) if wanted. Not yet in the weekly pipeline — new events appear as new ids each season.

- [x] (DONE 2026-08-29) Events-directory analytics for superadmin (/admin/events-analytics + /admin card): event_reg_clicks logs every Register/Official-Site click with was_gold snapshotted server-side at click time (historically-truthful gold-vs-standard comparison); daily_counters gains upcoming_events_views + upcoming_events_ics_exports; dashboard shows CTR, gold share, per-listing gold lift (the paid-gold sales number), per-event clicks/saves. Beacon is keepalive fire-and-forget; admins excluded from all counts.

- [x] (DONE 2026-08-27) Gold Register buttons rev-stream: ghost-by-default Register buttons; gold via organizations.is_sponsor (superadmin toggle, /admin/orgs) OR per-event org_upcoming_events.gold. One FREE gold per organizer (dashboard placement, movable; seeded on each org's earliest upcoming event local+prod), additional per-event purchases open Oct 15 2026 (synchronized market open). Neutral sorting principle + public disclosure footnote; organizer FAQ explains sustainability. Next when first purchase lands: pricing sheet, term tracking (gold_until), invoice workflow.

- [x] (DONE 2026-08-25) Claim-aware org public page: one template, three states. Unclaimed = unchanged (the invite demo: concierge coin pitch + organizer recruit closing). Claimed/public = "Official partner" hero chip, family-facing card section ("Every award here becomes a card worth sharing", approved logo or monogram in the mock), closing flips to "Dance with [Org]" + register CTA + archive link — the page markets THE ORG, not AwardHome. Claimed/owner = Owner's Box unchanged. Positive-signal-only (no unclaimed-state advertising).
- [x] (DONE 2026-08-25) Org visibility / objection readiness (phase 1): organizations.visibility ('public'/'unlisted'/'hidden') + visibility_note columns; superadmin select on /admin/orgs (with note-for-the-record prompt); 'unlisted' enforcement = org page 404s publicly (owner + superadmin still see it with a banner, no enumeration oracle) + homepage circuit card removed (cache refreshed); response playbook letters (convert → unlist → door open) in org_invite_draft.md Letter 3. Marquee file carries a manual-removal note.
- [ ] Org visibility phase 2 ('hidden' enforcement): exclude a hidden org's awards from ALL public surfaces — dancer/studio pages, leaderboards, search, widget, platform totals — while claimed owners still see their own records. State + UI already exist ('hidden' currently behaves as unlisted); build only if an objector escalates past unlisting. Surfaces to touch: award card queries (dancer/studio/year partials), orgsHistory/circuit, leaderboard aggregations in loadHomepageData, widget query, landing counts (manual).
- [x] (DONE 2026-08-25) Organizer navbar: org_owner role now gets Home / Dashboard / My Events / Public Page links via new /my-org + /my-org/public redirect routes (mirrors /my-studio); /manage/org/:id honors ?tab= deep links (My Events → past-events tab).
- [x] (DONE 2026-08-25) Organizer onboarding revamp: /manage/org/:id now computes checklist state (results uploaded/live, marketing profile, logo) — fresh orgs get a welcome header + three-step concierge checklist and land directly on the upload form (was buried two menu levels deep); partially-set-up orgs get a "finish setting up" strip until complete. Sub-nav renamed "Upload Results"; organizer FAQ updated; invite letter (utils/invites.js + org_invite_draft.md v4) refreshed to 1.1M awards / 3,200+ events / 17 competitions + three-step onboarding promise.
- [x] (DONE 2026-08-24) Homepage org-card demand telemetry: cards stay deliberately unlinked (org data low-profile until the org partners — recorded in CLAUDE.md deliberate decisions); clicks recorded via POST /api/org-card-click + dance_home_views impressions; CTR surfaces in /admin/orgs "Card Demand" column as outreach ammunition. See features.md §1b.
- [x] (DONE 2026-08-26, phase 1) Upcoming Events Directory ("Plan Your Season", ideas.md §7): org_upcoming_events table (owner rows authoritative over seed/scraped), dashboard Upcoming Events tab (add/edit/delete), public directory /dance/events (state/month/org filters, month groups, confirm-with-organizer note, org names unlinked per low-profile rule), "On Tour" org-page section, seed importer scripts/import_upcoming_txt.js + committed seed file. See features.md.
- [x] (DONE 2026-08-26) Upcoming Events seed: 827 tour stops for 16 of 17 orgs, hand-curated from official schedules via 3 parallel research agents into scripts/seed/upcoming_events_2026.txt, imported identically local+prod (idempotent; re-run inserts 0). Corrected org domains discovered en route: KAR=dancekar.com, DreamMaker=dreammakerdance.com, Nexstar=nexstarcompetition.com. Encore dates are from its DanceComps.com listing (official tour page empty) — refresh when populated.
- [ ] Epic Dance identity verification: epicdanceinc.com (12-stop 2027 tour, midwest) vs epicdanceshowcase.com are DIFFERENT companies — confirm which one AwardHome's "Epic Dance" org (0 events, claimed in testing) actually is before seeding its tour dates; the researched epicdanceinc.com rows are in the group-B agent output if it matches.
- [x] (DONE 2026-08-26) Upcoming Events phase 2: scripts/scrape_upcoming_events.js refreshes 10 DanceBug-family orgs weekly (Monday pipeline, staged DB); owner rows authoritative, dropped events auto-unlist, directory shows "last checked" date. Bespoke-site orgs (YAGP/NYCDA/Showstopper/StarQuest/Spotlight/Encore) stay seed+manual — add adapters if their dates drift.
- [x] (DONE 2026-08-26) Upcoming Events phase 3: near-me (browser geolocation + per-event coords — scraper-captured or one-time-geocoded into committed cache scripts/seed/city_coords.json, haversine sort, radius picker, location never stored), per-user shortlist (event_shortlists, star toggle + ?saved=1 filter), calendar export (/dance/events.ics of any filtered view or shortlist). Remaining idea: season digest email (shortlist reminders / new-events-near-you) — build when there are users to send it to.
- [ ] Per-org card linking: when an org partners/approves, flip its homepage card to a real /dance/org link (likely a flag on the organizations row).
- [x] (DONE 2026-08-24) Studio page "Rafters" design preview: alternate owner-conversion design on `?design=rafters` (views/studio_v2.ejs + public/css/studio_v2.css), same route/data/prefs/claim flows as classic; static concept mockup in design/studio_page_mockup.html. See features.md §2d.
- [x] (DONE 2026-08-24) Rafters CUTOVER: Rafters is the default on all public surfaces (landing = hybrid public3/, /dance = The Hall for non-admins, studio/org/dancer = v2 views); ?design=v0 = classic escape hatch everywhere, ?design=rafters = alias; preview ribbons removed; admins keep index_admin. Card default stays classic (registry decides separately).
- [x] (DONE 2026-08-24) Landing champions ticker fed from public/marquee_awards.txt (one top award per organizer, pipe-separated, # comments; empty file hides the ticker) — replaces the hardcoded all-KAR/Rainbow list that could read as favoritism to other organizers. Edit the file + deploy to change it.
- [x] (DONE 2026-08-24) Award vocabulary batch editor (superadmin): /admin/orgs/:id/award-vocab — batch-rename award_type/category per org or per event, mark top awards (awards.is_top_award). Marquee list corrected to real National Champions / Title Winners (Grand Lines = size category, Diamond = adjudication level, Pas de Deux = category — not awards).
- [x] (DONE 2026-08-25) docs/org_top_awards.md: per-org prestigious-award reference researched from all 15 orgs' official rules pages (4 parallel research agents) — ranked top awards, title vocabularies, adjudication-band ladders (marked NOT prestigious), division vocab, DB string mappings, cross-org trap warnings, per-org is_top_award marking shorthand. Note: "Encore" identity needs verification (multiple comps share the name).
- [ ] Award data cleanup pass (Q, manual): go through each org's award types/categories in the vocab editor, correct mislabeled values, and mark the true top awards per event; Spotlight/Encore/Epic/Nexstar still need marquee entries once their data supports one. Future: wire marquee/HOF picks to is_top_award.
- [ ] Rafters post-cutover: gather feedback from studio owners + org contacts on the live design; decide the rafters CARD variant default (/admin/settings); align landing/org marketing mock cards to the winning card design before invite letters go out; eventually delete classic views + v0 flag once confidence is total.
- [x] (DONE 2026-08-24) Dancer page Rafters chrome preview (/dancer/:uid?design=rafters, dancer_v2.ejs + dancer_v2.css — cards untouched) AND "rafters" award-card variant in the card-design registry (?card_design=rafters, card_rafters.css, third radio on /admin/settings). The two compose. See features.md §2d + §3b.
- [x] (DONE 2026-08-24) /dance homepage "The Hall" preview on /dance?design=rafters (index_v2.ejs + dance_home_v2.css layered on studio_v2.css): search-first hero with cached platform totals, restyled circuit cards (still unlinked + click-tracked), featured-studio marquee (hidden when none), full leaderboard machinery reused (same endpoints/partials), three-doors band. Admins see it under the flag too.
- [x] (DONE 2026-08-24) Landing "hybrid" third variation in public3/ (/?design=hybrid): original's two-column hero, two-color AwardHome wordmark, floating visual + glow orb — on the Front Door system (Cinzel, search-first, real numbers, ticker, doors).
- [x] (DONE 2026-08-24) Landing page "Front Door" redesign candidate in public2/ (static, self-contained — open public2/index.html to compare against landing/): search-first hero ("Your awards are already home"), real platform numbers, champions ticker, share-card fan, three doors, permanence section. Wired 2026-08-24: /?design=rafters serves it (bypasses the logged-in redirect for previewing); landing/ still serves / by default.
- [x] (DONE 2026-08-24) Org page "Rafters" organizer edition on /dance/org/:slug?design=rafters (org_v2.ejs + org_v2.css layered on studio_v2.css): reach hero, season chart, "The Coin" share-card mock (approval-gated logo), champions wall, year-tabbed archive, status-free organizer pitch. Use as the demo link in invitation letters once blessed. See features.md §2d.
- [x] (DONE 2026-08-13) Feature-flag release infrastructure: feature_flags table (off/beta/on + lazy scheduled flips), flagOn() cached helper, /admin/features release console, early_access beta cohort column; thank_you_notes + award_photos ship dark on prod migrate; all card surfaces and write endpoints flag-gated (absent flag = off). See features.md §3c.
- [x] (DONE 2026-08-13) Auto-moderation for thank-you notes behind the auto_moderation flag: rules (links/contacts/profanity — minor-PII protection) → trusted authors → free OpenAI Moderation API; moderation_mode manual/assisted/auto on /admin/settings; queue shows 🤖 verdicts; "Recently Auto-Approved" feed with one-click revoke (pulls identical copies). Photos stay human-reviewed. See features.md §3c.
- [ ] Release-flip follow-ups: "What's New" public changelog page + release announcement email to claimed users on each flag flip; early-access opt-in toggle for users (column exists, no UI); vision-moderation pre-filter for photos + trusted-uploader auto-approve.
- [x] (DONE 2026-08-20) Social reactions on award cards behind the `reactions` flag (ships dark): cheer/love chips on trophy-case cards, anonymous via 1-yr signed cookie, separate reactions.sqlite (write isolation + own litestream replica — restart litestream.service after the deploy that ships it). See features.md §3d.
- [ ] Reactions follow-ups when the flag flips on: FAQ entry (views/faq_dancer.ejs), counts on mini-card grid / leaderboards if wanted, weekly "your awards got N cheers" digest email idea (ideas.md §5).
- [ ] Show dancer vanity chip (#Tag) on homepage leaderboard rows and share cards (field: dancers.vanity_tag, chip style: .vanity-chip; add vanity_tag to the three dancer leaderboard queries in routes/dance/public.js and render in views/partials/leaderboard_rows.ejs). The #Tag itself shipped on dancer profiles 2026-08-08.
- [ ] Email notifications for studio admins when new pending roster/award verification requests arrive (profile claims routed by studio code DO email the director as of 2026-08-13; award claims and roster joins still don't).
- [x] (DONE 2026-08-13) Dancer profile claim studio-code routing: optional Studio Claim Code on the claim form; valid code → claim routed to the studio director's Verifications queue (email ping, director approval finalizes — no admin step); no/bad code → admin queue with ✓/✗ code badges. Decision emails to claimants from both paths (approval deep-links to card extras); approving auto-rejects competing pending claims. Also fixed pre-existing 500 on /manage/studio/:id/verifications (missing dancer_studios.created_at column).
- [ ] Director-initiated profile claims (inverse flow): director enters a parent's email on a roster dancer → pre-authorized claim link, zero review (pattern: org claim tokens).
- [ ] Testimonials for organizations, studios, and dancers (makes them stand out; FAQ pages and org marketing profiles are done — this is the missing piece).
- [ ] File storage to AWS S3 for uploads (org results, branding images currently on local disk; ties into the org result submission pipeline below).
- [ ] Organization competition result submission end-to-end: upload works (org_uploads table + local disk) but the S3 storage + admin review/batch-ingestion pipeline is not built. DESIGN CONSTRAINT (2026-08-23): org-supplied casts must land as suggestions diffed against the director's cast (preview-then-apply + verification queue), respect award_dancer_removals tombstones, and results-facts (placement/routine names) take precedence over user input while casts defer to directors. See features.md §2c.
- [ ] Mobile app as a PWA (MVP). The responsive web pass is done; PWA manifest/service worker/install flow is not.
- [x] (DONE 2026-08-12) Multi-page flip-book award card: paged back-stack (certificate → photo → acknowledgements → organizer colophon), per-dancer ack lines on group cards, all owner content superadmin-moderated at /admin/card-content, A/B design switch (classic/flipbook) at /admin/settings + ?card_design= session preview. See features.md §3b.
- [ ] Flip-book follow-ups: per-page share-image rendering (canvas/off-screen render — groundwork for auto-generated social video shorts, see ideas.md §3); email superadmin when new card content lands in the review queue; certificate-page photo medallion inset.

### IP / legal
- [ ] Patent triage: have an attorney review maybe_patentable.md (Tier A first); if we commit to the flip-book card (A3/A4), consider a provisional filing BEFORE deploying it publicly.

## DONE

### 2026-08-23 — Non-enumerable public studio URLs
- [x] Public studio surfaces now resolve by studios.unique_id (STU-<hex>-slug, already present + distinct on all 18,514 rows): /dance/studio, first-places, year-tab API, widget, claim pages. Numeric ids 404 with NO redirect (an id→uid redirect would be an enumeration oracle); legacy /studio/:id 301s removed for the same reason. ~45 link generators across 25 files converted (views, login redirects, invite emails, search API, leaderboards, admin consoles); smoke asserts numeric-404 + uid-200; full authenticated route audit clean. Documented as a deliberate decision in CLAUDE.md.

### 2026-08-23 — Refactor-damage audit (prompted by the lost Compare & Merge wiring)
- [x] Three-sweep audit for silent refactor breakage: (1) runtime — every GET route hit with authenticated superadmin/owner/org-owner sessions, zero 5xx (kept as scripts/audit_get_routes.js — run after big refactors); (2) static — every onclick handler in views vs defined JS functions (3 hits, all resolved by including pages); (3) static — every form/fetch URL vs registered routes (7 hits, all false positives; /beta-unlock lives in server.js). Verdict: the two already-fixed bugs (history bare app.locals, roster merge JS deletion) were the only refactor casualties.

### 2026-08-23 — Link provenance & tombstones (three-source reconciliation, phase 1)
- [x] award_dancers.source ('import'/'studio_owner'/'dancer_claim'/'admin') + created_at (DB-trigger-stamped) on every new link; legacy claim links recovered from status. award_dancer_removals tombstones written on all four director-removal surfaces; auto-backfill respects them; explicit claims still allowed (pending → director queue referees); director re-add clears. Group-dancers chips styled by provenance with legend. Merges carry provenance. See features.md §2c; org-upload design constraint recorded in TODOS.

### 2026-08-22 — Group Routine Dancers
- [x] Group Routine Dancers page for studio owners (/manage/studio/:id/group-dancers): paste-a-list cast entry per routine-year, preview-then-apply with roster matching (link / create / ambiguous-pick for same-name dancers), applies to every award the routine won that year; sidebar link on all manage pages + onboarding step + FAQ §8b. See features.md §2b.
- [ ] Group-dancers follow-ups: "copy cast from another routine/year" shortcut; extract the duplicated manage-sidebar markup (now 8 copies) into a partial with an active-item param.

### 2026-08-21 — studio-claim walkthrough fixes (user QA on studio 62)
- [x] QA test tenant for safe manual walkthroughs on prod: `scripts/qa_fixtures.js seed|remove|status` — transient org/studio/dancers/awards, all named "(please ignore)"; deliberate decision: NO is_test discovery filters (transience + naming instead of a filter every future query must remember). `remove` is surgical (fixture keys + qa-*@awardhome.com users) and self-verifies zero leftovers. Rules: register test accounts as qa-*@awardhome.com; never leave seeded overnight (featured cron).
- [x] Pending-claim visibility: login now lands pending studio claimants on their claimed studio's page (was: generic /dance); the studio page shows a "Verification & approval pending" banner instead of the Claim button; /my-dancers explains the pending studio claim instead of pushing the dancer flow.
- [x] /manage/studio/:id/history 500 fixed: bare `app.locals` reference left from the server.js modularization (threw for every owner whose studio had awards). Smoke suite now has an authenticated owner-flow section (temp user/studio/award fixtures, real login) covering history, awards editor, pending-claim states — 59 checks total.
- [x] "– #" routine names: Rainbow stores a literal placeholder for studio-level awards; scraper now blanks it, awards editor groups blank-name dancer-less awards as "Studio Awards" (dancer awards missing names stay "Unspecified Routine"), 143 rows cleaned.
- [x] Duplicate award repair: Rainbow importer's idempotency key included derived award_class, so classifier changes re-inserted whole events — 8,838 duplicate awards (37 Rainbow events + 3 StarQuest) removed by scripts/dedup_reimported_awards.js (idempotent, deterministic keeper choice, child rows repointed). Importer key now observable-fields-only + follows studio merges + updates award_class in place. RUN ON PROD after deploy: `node scripts/dedup_reimported_awards.js`.

### 2026-08-04 → 08-09 sprint (security, deploy, beta launch — previously untracked)
- [x] P0 security lockdown: admin/merge/backfill/feature endpoints require admin; random single-use 24h email-verification tokens; session hardening (SESSION_SECRET, custom SQLite store, 7-day cookies); login/register rate limits.
- [x] Owner-check middleware refactor: requireStudioOwner / requireOrgOwner replace ~40 inline checks.
- [x] App modularization: server.js (3,700 lines) split into routes/{auth,admin,feedback} + routes/dance/{claims,orgs,studios,dancers,public} + middleware; landing page at /, public dance routes under /dance with 301s from legacy URLs; central error handler; /healthz.
- [x] DB hardening: shared WAL connection (busy_timeout, synchronous=NORMAL); schema drift sync; orphaned-FK repair (all 153 awards.dancer_id restored); weekly PRAGMA foreign_key_check cron reporting to Sentry.
- [x] Smoke test suite (npm run smoke, 46 checks: public renders, auth lockdown, CSRF flows).
- [x] Production deployment: EC2 + systemd + nginx kit with deploy.sh (pull → npm ci → migrate → restart → healthz); Litestream continuous backup to S3 + passed restore drill; nightly local backup cron; HTTPS via certbot; Cloudflare (proxied DNS, real-IP for rate limits, Email Routing); Resend domain verified; stable EIP.
- [x] Observability: Sentry error tracking + morgan request logging.
- [x] Performance: homepage aggregation cache (2.6s → 30ms); leaderboards lazy-load beyond top 25 (2MB → 150KB); composite award indexes.
- [x] npm audit: 12 vulnerabilities (incl. 1 critical) → 0.
- [x] CSRF protection site-wide (session tokens + auto-injecting client script) + helmet security headers (CSP/HSTS, widget embeds preserved) + multer upload limits/type filters.
- [x] Black & gold design system unified across app and landing (two-tier gold, neutral graphite cards).
- [x] Featured-studio engine: activity-based auto-rotation (verified-action weighting, decay, tenure/cooldown), nightly cron, public policy FAQ; leaderboards stay unpaid (committed policy).
- [x] Invite cockpit (/admin/marketing/studios): award-count/rank filters, inline contact edit, per-row preview + send, rank-aware email copy, invite tracking, suppression list, RFC 8058 one-click unsubscribe.
- [x] Private beta gate (BETA_MODE) over /dance + /dancer with magic-link unlock embedded in invite emails.
- [x] One-page public claim flows for studios AND dancers (account + claim in one submit, deferred domain fast-track auto-approval after email verification); email deliverability fixes.
- [x] Post-claim onboarding checklist for studio owners.
- [x] Rate-limited hero typeahead search for studios and dancers.
- [x] Mobile responsive pass (nav collapse, WebP hero, 320px fixes).
- [x] Dancer vanity #Tag (dancer-chosen, next to immutable ID) on dancer profiles.
- [x] Dancer trophy case polish: year-first timeline, award-card flip easter egg (champagne certificate back), portrait/landscape/table three-way view toggle.
- [x] Data cleanup — garbled scraped studio names (scripts/cleanup_garbled_studios.js): 3 category-header pseudo-studios deactivated, 55 en-dash result-line studios merged via dancer/event evidence, 2 renamed; /dance/studios top now clean.
- [x] Data cleanup — tab-name dedup (scripts/dedup_tab_studios.js): 566 StarQuest tab-variant studios merged into proven twins (≥2 shared dancer names required — same-named studios in different places are NOT assumed identical), 1,544 renamed tabs→spaces, 553 unproven kept separate + flagged.
- [x] Studio directory (/dance/studios) admin-gated to prevent bulk scraping; public discovery via search + leaderboards + featured; landing CTA repointed to hero search.
- [x] Dancer award card scales like an image (2026-08-09): fixed-aspect card is a CSS container, all internal sizing in cqw — fonts/padding/radii/icons shrink proportionally with card width on mobile; rem fallback for old browsers.
- [x] Mini-card overview for big trophy walls (2026-08-09): 4th display mode "Mini" — compact tier-icon+placement grid per year, tap opens the full card in a lightbox (which keeps tap-to-flip); auto-default on phones for dancers with 12+ awards.
- [x] Staged import gate (2026-08-11): weekly cron now imports into a snapshot (staging_import.sqlite via sqlite .backup; DB_PATH env override in openDb + self-connecting importers), scripts/validate_import.js scores each new/changed event against the org's own history (award-count percentiles, place vocabulary, category novelty, brand-new-studio share, junk name shapes, idempotency-break ratio) → green auto-promotes (deterministic cache replay to live), amber/red HOLDS with reports/import_review_*.md + email to REVIEW_EMAIL/SUPERADMIN_EMAIL, approve via weekly_update.js --promote. AUTO_PROMOTE=never forces manual approval of green too; LLM_REVIEW=true appends a claude -p second opinion. Live DB is untouched until promotion — validates the DB delta, not the txt (a txt gate would have missed the 08-11 studio-duplication importer bug).
- [x] Post-May data catch-up (2026-08-10/11, local+prod at parity): web orgs +65 events/+36.7k awards via weekly_update.js (KAR/Rainbow/Starpower/YAGP nationals incl.); StarQuest PDFs +12 events/+4,194 awards (Galveston/Hershey/Orlando nationals; first re-import created 34,956 dup awards + 1,778 dup studios via tab-named studio lookups — purged, importer now resolves raw→collapsed names and follows merges). NYCDA: 25-26 regionals were already complete; Buffalo/Dallas/Greenville/Savannah "Results" PDFs are running-order schedules (no results published) — quarantined NOT-, 4 empty events deleted; July 2026 nationals not yet posted. Showstopper 2026 not yet posted. Pre-existing local-only quirk: local has 669 Revolution awards prod lacks.
- [x] Weekly award-data updater (2026-08-10, scripts/weekly_update.js + server.js cron Mon 5AM behind ENABLE_WEEKLY_SCRAPE): two-tier freshness — discovery pages (event lists/sitemaps) refetched every run; result pages refetched while first-seen < --window days (scrape_log, seeded from cache mtimes), then frozen; content hashes flag late edits (insert-only imports don't auto-correct changed rows). Post-steps scoped to new events only: heuristic first-place marking (utils/first_place.js, shared with admin audit pages), org_first_place_rules, dancer backfill. PDF orgs download-only + "awaiting QA" report (downloaders got URL manifests — the filename-collision loop used to re-download everything as -1.pdf). Also fixed post-modularization path breakage: fetch_cache raw/, tobeprocessed/, and 14 scripts opening ./database.sqlite relative to cwd.
- [x] Data cleanup — StarQuest scattered-space categories (2026-08-10, scripts/cleanup_starquest_names.js, applied local+prod identically): PDF fragment joining had scattered spaces mid-word ("Adult S ol o Award") and left tabs between header columns, making one real category look like many single-use ones. utils/normalize_names.js reconstructs true spacing from content; 512→316 categories, 246→146 award types, 42,671 award fields rewritten, superadmin's 12 first-place rules deduped and ported to prod (802 firsts both sides). Extractor + importer now normalize at the source. Same damage likely exists in StarQuest routine/dancer/choreographer names — out of scope here.
- [x] Persistent org-level first-place rules (2026-08-10): org_first_place_rules table; the /admin/org/:slug/categories toggle now upserts a rule AND updates awards; scripts/apply_first_place_rules.js re-applies rules after imports (--org / --event scoping; --seed-showstopper imported the 95 pre-rules Showstopper combos — applied locally, 0 drift). Run the seed + deploy on prod. Event-level toggles remain direct overrides.

### Earlier features (previously untracked)
- [x] Global feedback system with admin replies + My Feedback dashboard.
- [x] Hall of Fame curation for studio admins.
- [x] AI marketing summary generator (OpenAI) with edit tracking, AI Summaries library tab, and superadmin model-switching settings dashboard.
- [x] Organization history analytics dashboard with drill-down award details and two-step text summary generator.
- [x] Unified mailer supporting Gmail and Resend (EMAIL_PROVIDER switch).
- [x] StarQuest and Showstopper offline PDF extraction + ingestion pipelines.

### Bootstrap-era TODOs (all shipped)
- [x] Initialize Node project and install dependencies
- [x] Create SQLite schema in `database.js`
- [x] Build `scrape_dancekar.js`
- [x] Build `server.js` and Express routes
- [x] Build EJS views (`layout`, `index`, `studio`, `dancer`)
- [x] Style the application to be premium and modern
- [x] Implement Dancer Public Profile Pages (`/dancer/:unique_id`) with verified awards and stats
- [x] Add optional Email-Backed Accounts for dancers to secure their unique IDs (dancer claim flow + verified accounts)
- [x] Add Bulk Actions (Approve/Deny Selected) to the Studio Verification Dashboard (awards + roster tabs)
- [x] Implement Roster Export to CSV for Studio Admins (/manage/studio/:id/roster/export)
- [x] Add Composite Indexes to the SQLite database (e.g., `event_id, performance_name, studio_id`) to optimize backfill and search queries at scale
- [x] Implement Server-side Pagination/Lazy Loading for studio awards lists (year tabs load on demand via partials/studio_year_events)
- [x] Create automated nightly backups for the SQLite database (3am cron keep-last-7 + Litestream continuous S3 replication)
- [x] Add API Rate Limiting to `/api/claim-award` to prevent brute-forcing of Join Codes
- [x] Verify email-based claim processes (verified end-to-end in production 2026-08-08, incl. deliverability fixes)
- [x] Suggest best DB for production (decision 2026-08-04: keep SQLite + WAL + Litestream; revisit Postgres only at real scaling signals)
- [x] On Dancer page, list awards by year (year-first timeline with year dividers)
- [x] add to superadmin dashboard: number of studios have more than 15 awards, number of studios have email addresses
- [x] Make sure resend API key and email function works
- [x] Create project documentation files

### Pre-sprint DONE (original list)
- [x] Rearrange columns on /admin/studios to put 'Featured' on the rightmost side.
- [x] Add search filter by studio name to /admin/studios.
- [x] Add is_claimed field to studios table.
- [x] Display Claim/Login buttons on individual studio pages based on claim status.
- [x] Add 'Why Claim' modal to studio page detailing benefits (embed widget, customize profile, manage awards, analytics).
- [x] Add dotenv and bootstrap Superadmin account via environment variables.
- [x] Create User Management dashboard for superadmin to toggle admin roles.
- [x] Build Phase 1 of Studio Management Dashboard (Profile Editing & Basic Analytics).
- [x] Build Phase 2: Awards Editor with empty-field locking and many-to-many group dancer mapping.
- [x] Build Phase 2: Widget Builder with custom theme and color pickers.
- [x] Phase 3: Optimize Awards Editor performance using Single Edit Modal and Year-Based Pagination.
- [x] NYCDA PDF Bulk Extraction: Developed coordinate-based parsing script (`categorize_nycda.js`) to parse all 2022+ competition and convention results from NYCDA PDFs into structured text, successfully marking valid files with `GOOD-`.
- [x] Integrate Instagram and TikTok handles into Studio Profiles and Management Dashboard.
- [x] Create a `.gitignore` file for the repository.
- [x] Build automated ETL Studio Data Bootstrapping pipeline with staging tables and admin review dashboard.
- [x] Design for dancers ease of use: view/search dancers by name, join studio via code, claim group award via unique ID, color-coded badges.
- [x] Build Organizer Custom Branding Dashboard (Live Trophy Preview, Logo sliders, Taxonomy-Based Custom Icons, Legal Agreements).
- [x] Implement Organizer Marketing Profile (Slogan and Description).
- [x] Refactor Organizer Overview to Tabbed UI with Client-Side Search for past events.
- [x] Phase 2: Bulk CSV Self-Reporting with Automatic Roster Linking for missing awards.
- [x] Adjust Studio Dashboard Analytics to count total Scholarships & Invites.
- [x] Add 'Invitation' default custom icon field for Organizer Custom Branding.
- [x] Finalize Embeddable Studio Widgets (Cross-Origin Headers, Many-to-Many Group Dancers, Custom Data Filters).
- [x] Build Dancer Dashboard: Find Missing Awards search tool and Smart Auto-Backfill claiming.
- [x] Enhance Studio Roster UI: Active/Inactive tabs, status toggles, and recent awards summary popup.
- [x] Add dynamic milestone and celebratory banner for top-performing dancers on public profiles.
- [x] Implement Awards Editor advanced grouping (by Event, Routine, Dancer) with multi-year sorting capabilities for Studio Admins.
